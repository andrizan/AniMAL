name: Build & Release APK

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    environment: build-config
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - run: flutter pub get

      - name: Create .env from secrets
        run: |
          echo "MAL_CLIENT_ID=${{ secrets.MAL_CLIENT_ID }}" >> .env
          echo "MAL_CLIENT_SECRET=${{ secrets.MAL_CLIENT_SECRET }}" >> .env
          echo "MAL_REDIRECT_URI=${{ secrets.MAL_REDIRECT_URI }}" >> .env

      - run: flutter build apk --release

      - name: Rename APK
        run: mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/animal-${{ github.ref_name }}.apk

      - uses: softprops/action-gh-release@v2
        with:
          files: build/app/outputs/flutter-apk/animal-${{ github.ref_name }}.apk
          generate_release_notes: true
